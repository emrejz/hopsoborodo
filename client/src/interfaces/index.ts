export interface ITopLinkItem {
  title: string;
  path: string;
}

export enum ESignInputCase {
  start = "start",
  error = "error",
  success = "success",
}

export enum ESignButtonType {
  username = "username",
  password = "password",
  cPassword = "cPassword",
}
