import React, { useContext } from "react";
import { Block, Text } from "styledComps";

import "./index.scss";

interface IProps {}
const Home: React.FC<IProps> = () => {
  return (
    <Block>
      <Text>emre</Text>
    </Block>
  );
};

export default Home;
